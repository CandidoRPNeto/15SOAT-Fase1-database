# Placeholder — sem provider declarado ainda. O provider Dokploy (comunitário)
# e sua versão pinada, mais o recurso Database real, entram no epic 2
# (RFC-002). Este arquivo só existe para dar ao workflow de CI algo válido
# para `terraform init`/`validate` desde o epic 1.
terraform {
  required_version = ">= 1.5"
}
